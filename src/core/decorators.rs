pub fn lowercase () {
	
}

pub fn uppercase() {

}

pub fn del_space() {

}

/*

def lowercase(func):
    def wrapper(*args, **kwargs):
        val = func(*args, **kwargs)
        if isinstance(val, str):
            val = val.lower()
        elif isinstance(val, list):
            val = [elem.lower() for elem in val]
        return val
    return wrapper


def uppercase(func):
    def wrapper(*args, **kwargs):
        val = func(*args, **kwargs)
        if isinstance(val, str):
            val = val.upper()
        elif isinstance(val, list):
            val = [elem.upper() for elem in val]
        return val
    return wrapper


def del_space(func):
    def wrapper(*args, **kwargs):
        val = func(*args, **kwargs)
        if isinstance(val, str):
            val = val.replace(' ', '')
        elif isinstance(val, list):
            val = [elem.replace(' ', '') for elem in val]
        return val
    return wrapper

*/